<%= bootstrap_form_for @user do |f| %>
    <%= f.text_field :email %>
    <%= f.select :role, User.roles.keys.map { |k| [k, k]} %>
    <%= f.primary %>
    <%= link_to 'Back', users_path, class: 'btn btn-white' %>
<% end %>