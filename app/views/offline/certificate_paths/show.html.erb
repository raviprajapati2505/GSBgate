<div class="row">
  <div class="col-md-4">
    <div class="ibox float-e-margins">
      <div class="ibox-title">
        <h5><%= t('.certificates.title') %></h5>
      </div>
      <div class="ibox-content">
        <table class="table table-bordered table-striped project-details-table">
          <tbody>
            <tr>
              <th><%= Offline::CertificatePath.human_attribute_name('name') %></th>
              <td><%= @certificate_path.name %></td>
            </tr>
             <tr>
              <th><%= Offline::CertificatePath.human_attribute_name('version') %></th>
              <td><%= @certificate_path.version %></td>
            </tr>
             <tr>
              <th><%= Offline::CertificatePath.human_attribute_name('rating') %></th>
              <td><%= @certificate_path.rating %></td>
            </tr>
             <tr>
              <th><%= Offline::CertificatePath.human_attribute_name('development_type') %></th>
              <td><%= @certificate_path.development_type %></td>
            </tr>
             <tr>
              <th><%= Offline::CertificatePath.human_attribute_name('status') %></th>
              <td><%= @certificate_path.status %></td>
            </tr>
             <tr>
              <th><%= Offline::CertificatePath.human_attribute_name('score') %></th>
              <td><%= @certificate_path.score %></td>
            </tr>
             <tr>
              <th><%= Offline::CertificatePath.human_attribute_name('certified_at') %></th>
              <td><%= @certificate_path.certified_at %></td>
            </tr>
          </tbody>
        </table>
        <%= btn_link_to(:back, icon: 'backward', text: 'Back') %>
        <%= btn_link_to(edit_offline_project_certificate_path(@project), icon: 'edit', text: 'Edit') %>
        <%= btn_link_to(confirm_destroy_offline_project_certificate_path(@project, @certificate_path), {icon: 'trash', text: 'Delete', style: 'danger', method: :get, data: {confirm: 'Do you really want to delete this project?'}}) %>
      </div>
    </div>
  </div>
  <div class="col-md-8">
    <div class="ibox float-e-margins">
      <div class="ibox-title">
        <h5><%= t('.schemes.title') %></h5>
      </div>
      <div class="ibox-content">
      <table class="table table-bordered table-mb-1">
        <tbody>
            <tr class="bg-header-grey">
              <th>Scheme Name</th>
              <th>Weighting (%)</th>
            </tr>
            <% unless @certificate_path.offline_scheme_mixes.nil? %>
              <% @certificate_path.offline_scheme_mixes.each do |scheme_mix| %>
                <tr>
                  <td><%= link_to scheme_mix.name, offline_project_certificate_scheme_path(@project, @certificate_path, scheme_mix) %></td>
                  <td><%= scheme_mix.weight %></td>
                </tr>
              <% end %>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
