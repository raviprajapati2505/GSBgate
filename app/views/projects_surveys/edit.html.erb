<div class="ibox">
    <div class="ibox-content">
      <h2><%= t('project_surveys.edit.title' ) %></h2>
        <%= bootstrap_form_for(@projects_survey, url: project_survey_path(@project, @projects_survey.id), method: 'put') do |f| %>
         <%= render partial: 'form', locals: {survey_types: @survey_types, f: f, project_survey: @projects_survey } %>
        <% end %>
  </div>
</div>