<div class="col-lg-12">
  <% survey_question.question_options.each do |option| %>
    <%= question_response.radio_button :value, option.option_text,class: 'custom-radio', required: survey_question.mandatory ? true : false, label: option.option_text %>
  <% end %>
</div>