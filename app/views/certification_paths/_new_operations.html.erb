<%= bootstrap_form_for(@certification_path, url: apply_certification_path_path(@project, @certification_path.certificate)) do |f| %>

    <!-- Set the certificate-->
    <%= f.hidden_field :certificate_id, value: @certification_path.certificate_id %>

    <%= f.select :certificate_id, options_for_select(Certificate.operations_certificate.uniq.map {|certificate| [certificate.gsas_version, certificate.id]}, @certification_path.certificate_id), label: 'GSAS version' %>

    <!-- Scheme Mix -->
    <%= f.static_control label: 'Scheme' do %>
        <%= @certification_path.scheme_mixes.first.name.to_s.humanize %>
    <% end %>

    <%= btn_save %>
<% end %>
