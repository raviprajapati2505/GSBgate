$(function () {
    var container = $('.app_container_1');
    var project_id = container.data('project-id')
    container.append('This data was inserted by project_tools_library.js. <br/>TODO: replace with visualization for project id: ' + project_id)
});